export class Cube {

    id: number
    date: string
    cubeTexts: string[]

    constructor(id:number, date: string, cubeTexts: string[]) {
        this.id = id
        this.date = date
        this.cubeTexts = cubeTexts
    }
}